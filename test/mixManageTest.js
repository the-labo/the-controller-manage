/**
 * Test for mixManage.
 * Runs with mocha.
 */
'use strict'

const mixManage = require('../lib/mixManage')
const { ok, equal } = require('assert')

describe('mix-manage', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
